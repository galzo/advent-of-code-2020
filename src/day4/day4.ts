import { readFileAsStrings } from "../common/FileReader";

const parsePassportsData = (rawData: string[]) => {
    
};


const getPassportsData = async () => {
    const rawData = await readFileAsStrings("src/day4/input/day4.txt");

};